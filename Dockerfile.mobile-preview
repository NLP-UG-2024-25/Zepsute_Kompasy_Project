# Dockerfile for Mobile Preview Frame
# Shows your app inside a phone-shaped frame with different device sizes

FROM nginx:alpine

# Copy custom nginx config
COPY nginx-mobile-preview.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
